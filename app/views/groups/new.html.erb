 <%= form_with model: @group do |f| %>
      <div class="form-group">
        <%= f.label :name %><br>
        <%= f.text_field :name, class: 'form-control group-name' %>
      </div>
      <div class="form-group">
        <%= f.submit %>
      </div>
<% end %>